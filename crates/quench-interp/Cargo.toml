[package]
name = "quench-interp"
description = "Running QIR directly, as the engine that does the least."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
quench-text = { path = "../quench-text" }
quench-qir = { path = "../quench-qir" }
quench-num = { path = "../quench-num" }
quench-heap = { path = "../quench-heap" }

[dev-dependencies]
quench-dev = { path = "../quench-dev" }
