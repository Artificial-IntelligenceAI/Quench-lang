[package]
name = "quench-heap"
description = "Where a running program's values live, and how they stop living."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
quench-qir = { path = "../quench-qir" }
quench-num = { path = "../quench-num" }
